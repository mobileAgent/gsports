- page = 'na' if page.nil?
- collection ||= nil
   
- if collection && collection.total_pages > 1
  .mailActions    
    .clearDiv
  %div{:style => 'float:right'} 
    = will_paginate collection, {:container => false, :separator => ' | '} 
    
    -# Add these options to remove the previous/next links 
      :prev_label => '', :next_label => ''}
    
    .clearDiv
    