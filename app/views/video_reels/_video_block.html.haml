#video_thumbnail
  = "<a href='#{video_reel_path(:action => 'show', :id => video_reel)}'><img src='#{@vidavee.file_thumbnail_medium(video_reel.thumbnail_dockey)}' title='#{h video_reel.title}'/></a>"
  %br
  = h video_reel.title
  %br
  = "#{h video_reel.description} - #{h video_reel.video_length}"
  %br
  
  - if current_user.admin? || current_user.id == video_reel.user_id
    %br
    = link_to 'Edit', edit_video_reel_path(video_reel)
    = link_to 'Destroy', video_reel, :confirm => 'Are you sure?', :method => :delete
