.profileContentArea.listingPage
  .profilePageHeader
    %h2.columnDescriptor
      %span
        =link_to "MESSAGES", sent_messages_path
      = @title || "Sent Messages"

  %script
    function hMessageAction(action) {
    var f = document.forms['message_form'];
    if (action=='delete') {
    if(window.confirm('Are you sure you want to permanently delete these messages?')) {
    ="f.action='#{url_for(:controller => 'sent_messages', :action => 'delete_multi')}';"
    f.submit();
    }
    }
    return false;
    }
    
  -form_for :message, :url => { :action => "new" }, :method => "post", :html => {:name => "message_form"} do |f| 
    .mailContent
      = render :partial => 'messages/tab_heading', :locals => {:page => 'sent', :collection => @sent_messages}
        
      .mailContainer  
        .mailNew
          = render :partial => 'sent_messages/sent_message_listing', :collection => @sent_messages

          %div
            \&nbsp;
            %br/
            %br/
          .forumIEfix
            \&nbsp;
            
            
            
          .clearDiv
        .clearDiv
        .clearDiv
      = render :partial => 'messages/tab_footer', :locals => {:page => 'sent', :collection => @sent_messages}
    .clearDiv