- load_features()

.landingContentArea
  /
    col1 content
  .mainCol1
    .contentBox.tanBackground
      .contentBoxHeader.yellowHeader
        %span.moreLink
          %a{ :href => "" }
            more>>
        %h2.boxDescription
          WHAT MY FRIENDS ARE WATCHING
      .contentBoxContent
        - for video in @network_favorites
          .featureSummary
            .featureSummaryThumb.favFIT
              %img{ :src => "#{@vidavee.file_thumbnail_medium(video.thumbnail_dockey)}" }
            .featureText
              %h3
                =h video.title
              %span.sportName
                =truncate_words(h(video.description),15)
                %br/
              %span.date
                =game_date(video.created_at)
              %br/
            .clearDiv
    .contentBox.tanBackground
      .contentBoxHeader.yellowHeader
        %span.moreLink
          %a{ :href => "" }
            more>>
        %h2.boxDescription
          MY FRIENDS RECENT CLIPS & HIGHLIGHTS 
      .contentBoxContent
        - for video in @network_recent
          .featureSummary
            .featureSummaryThumb.favFIT
              %img{ :src => "#{@vidavee.file_thumbnail_medium(video.thumbnail_dockey)}" }
            .featureText
              %h3
                = video.title
              %span.sportName
                =truncate_words(h(video.description),15)
              %br/
              %span.date
                =game_date(video.created_at)
              %br/
            .clearDiv
  /
    col2 content
  .mainCol2
    .contentBox.tanBackground
      .contentBoxHeader.yellowHeader
        %span.moreLink
          %a{ :href => "" }
            more>>
        %h2.boxDescription
          MOST POPULAR VIDEOS
      .contentBoxContent
        - for video in @popular_videos
          .featureSummary
            .featureSummaryThumb.favFIT
              %img{ :src => "#{@vidavee.file_thumbnail_medium(video.thumbnail_dockey)}" }
            .featureText
              %h3
                =h video.title
              %span.sportName
                =truncate_words(h(video.description),15)
              %br/
              %span.date
                =game_date(video.created_at)
              %br/
            .clearDiv
    .contentBox.tanBackground
      .contentBoxHeader.yellowHeader
        %span.moreLink
          %a{ :href => "" }
            more>>
        %h2.boxDescription
          RECENT FULL GAME UPLOADS
      .contentBoxContent
        - for video in @recent_uploads
          .featureSummary
            .featureSummaryThumb.favFIT
              %img{ :src => "#{@vidavee.file_thumbnail_medium(video.thumbnail_dockey)}" }
            .featureText
              %h3
                =h video.title
              %span.sportName
                =truncate_words(h(video.description),15)
              %br/
              %span.date
                =game_date(video.created_at)
              %br/
            .clearDiv
  /
    col3 content
  .mainCol3
    - team_posts(@user.team_id).each() do |post|
      .rightContentItem
        %span.moreLink
          = link_to 'more>>', user_post_path(post.user, post), :rel => 'bookmark'
        %h2
          = post.title
        = truncate_words(post.post, 75, '...' )


                
                
                
                