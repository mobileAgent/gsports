.profileContentArea.listingPage
  .profilePageHeader
    %h2.columnDescriptor
      %span
        Admin Dashboard
      - if @league
        = " for #{@league.name}"
  .leftNavCol
    %h2.yellowHeader
      = _('My Profile')
    .leftNav
      =render :partial => "users/profile_nav"
  .listings
    .listingItem
      =render :partial => 'shared/admin_nav'