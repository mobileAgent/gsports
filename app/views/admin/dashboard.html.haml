.profileContentArea.listingPage
  .profilePageHeader
    %h2.columnDescriptor
      %span
        =link_to 'ADMINISTRATION', :action => 'dashboard'
      Dashboard
      - if @league
        = " for #{@league.name}"
        
  =render :partial => "users/profile_nav"
  .listings
    .listingItem
      =render :partial => 'shared/admin_nav'