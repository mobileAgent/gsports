.profileContentArea.listingPage
  .profilePageHeader
    %h2.columnDescriptor
      %span
        Admin Dashboard
      - if @league
        = " for #{@league.name}"
        
  =render :partial => "users/profile_nav"
  .listings
    .listingItem
      =render :partial => 'shared/admin_nav'